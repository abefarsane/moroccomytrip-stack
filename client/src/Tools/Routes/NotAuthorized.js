import React, { Component } from 'react';
import './info-page.css'

export default function NotAuthorized() {

    return (
        <div className='not-auth-page'>
            <h1>You don't have the required permission to access this page.</h1>
        </div>
    )
}